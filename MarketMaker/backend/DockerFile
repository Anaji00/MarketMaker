# Use the official Python 3.11 slim image as the base image to keep the container small
FROM python:3.11-slim

# Set the working directory inside the container to /app
WORKDIR /app

# Install system dependencies required for building Python packages (specifically psycopg2)
# - gcc: Compiler required for compiling C extensions
# - libpq-dev: Header files for PostgreSQL
# We clean up the apt list afterwards to reduce the final image size
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    libpq-dev \
    && rm -rf /var/lib/apt/lists/*
    
# Copy requirements.txt first to leverage Docker layer caching
COPY requirements.txt .
# Install Python dependencies without storing cache to save space
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the application source code
COPY app ./app

# Command to run the application using Uvicorn server
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]